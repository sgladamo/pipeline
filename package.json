{
  "name": "pipeline",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "cd apps/web && npm start",
    "build:web": "cd apps/web && npm run build",
    "dev:api": "cd apps/api && dotnet run --project SA.Shield.Api",
    "build:api": "cd apps/api && dotnet build"
  },
  "devDependencies": {
    "prettier": "^2.8.0",
    "eslint": "^8.0.0"
  }
}
